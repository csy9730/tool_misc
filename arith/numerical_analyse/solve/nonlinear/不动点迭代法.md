# 不动点迭代法
### cobweb
cobweb 蜘蛛网
$$
Dfg(x)=f(x)-g(x) \\
fg(x) = f(g(x))\\
Rfg(x) = f(x)/g(x)
$$

需要分析Dfg函数的单调性，围绕着Dfg函数的过零点分析，分析两侧的单调性，

针对零点的邻域分析。
- 稳定零点
	- 双侧收敛，迭代会跨过零点两侧
	- 单侧收敛，迭代不会跨过零点两侧，（包含首次跨过零点，后续不跨过零点）
- 非稳定零点
    - 单侧放散，迭代不会跨过零点两侧
    - 双侧发散，迭代会跨过零点两侧


### 不动点迭代法
